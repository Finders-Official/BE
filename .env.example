# ========================================
# Finders API 환경 변수
# ========================================
# 사용법:
#   1. 이 파일을 .env로 복사: cp .env.example .env
#   2. 값 수정 후 사용
#   3. .env 파일은 Git에 커밋하지 마세요!
#
# 환경별 설정:
#   - Local: 기본값 그대로 사용 (Docker MySQL)
#   - Prod: GitHub Secrets 또는 GCP Secret Manager
# ========================================

# ========================================
# Spring Profile
# ========================================
SPRING_PROFILES_ACTIVE=local

# ========================================
# Database (MySQL)
# ========================================
# Local: Docker MySQL 기본값
# Prod: GCP Cloud SQL 연결 정보
DB_URL=jdbc:mysql://localhost:3306/finders
DB_USER=finders
DB_PASSWORD=finders123

# ========================================
# JWT
# ========================================
# Prod에서는 반드시 변경!
# 생성: openssl rand -base64 48
JWT_SECRET=your-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256

# ========================================
# OAuth2 - Kakao
# ========================================
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=http://localhost:8080/api/auth/kakao/callback

# ========================================
# OAuth2 - Apple
# ========================================
APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_REDIRECT_URI=http://localhost:8080/api/auth/apple/callback

# ========================================
# Google Cloud
# ========================================
GOOGLE_CLOUD_PROJECT=
GOOGLE_APPLICATION_CREDENTIALS=
GCS_BUCKET=
VISION_ENABLED=false
